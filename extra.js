function longitude(secuencia) {
  return secuencia.length; 
}

function rango(s, e) {
  let r = [];
  for (let i = s; i <= e; i++) {
     r.push(i)
  }
  return r;
}

function imprimir(s) {
  console.log(s)
}

function colocar(a, e) {
   array.push(e);
}

function absoluto(x) {
  return Math.abs(x)
}

function convertirEmMaiuscula(x) {
  return x.toUpperCase()
}

function comecaCom(x, y) {
  return x.startsWith(x, y);
}
